/**
 * VIVIM Intelligence Substrate — Content Blocks (K0-L0)
 * @module intel/executor/content-blocks
 * Migrated from src/executor/content-blocks.ts (P0-3).
 */
// Re-export from the canonical schema — this file exists only for import compat.

export type { ContentBlock } from '../schema/streaming.js'

