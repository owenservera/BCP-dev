/**
 * VIVIM AI Gateway — OpenAI-Compatible Stream Parser (LEGACY SHIM)
 * @module ai/protocol/openai-compatible/stream-parser
 *
 * Re-export shim. Canonical implementation now lives at
 * `src/plugin-kernel/adapter/impl/openai-compatible/stream-parser.ts`.
 */

export { parseSSEStream } from '../../../plugin-kernel/adapter/impl/openai-compatible/stream-parser.js'
