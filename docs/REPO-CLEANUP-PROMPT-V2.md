# MASTER PROMPT V2 — BCP-dev REPOSITORY CLEANUP & TRUTH RECONCILIATION (INCREMENTAL)

> V1 executed twice: owner cleanup `1a0293d` + agent cleanup `becb920`.
> V2 is NOT a re-run. It is an incremental reconciliation: verify V1 held,
> classify only what is new since `becb920`, advance open conflicts, and
> confirm the resumption gates. Full-archaeology sections from V1 are
> referenced, not repeated — re-do them only where V2 names new evidence.

You are a **Repository Cleanup / Archaeology Agent** working on:

```text
https://github.com/owenservera/BCP-dev
```

Current known baseline at assignment:

```text
HEAD: becb920 (agent cleanup) — verify with git log before starting
Prior: a528ffd (PROMPT-4-INTERRUPTED preservation) · 1a0293d (owner cleanup) · 18242a3 (prompts 1–3)
```

Your mission is **NOT to redesign BCP, Omega, or VIVIM**.

Your mission is to verify and extend the cleanup so a fresh agent can reliably distinguish:

```text
CURRENT
LAW / RATIFIED HISTORY
HISTORICAL / ARCHIVED
RAW RESEARCH
```

without being misled by stale plans, superseded architectures, duplicate documents, abandoned experiments, or old agent instructions — **including any material created after `becb920`**.

---

# 1. PRIMARY OBJECTIVE (UPDATED)

Confirm the V1 end state still holds, then reconcile only the delta:

```text
                    BCP-dev
                       │
          ┌────────────┼────────────┐
          ▼            ▼            ▼
       VIVIM          BCP            Ω
        MINE       CONTROL+        TARGET
                   MIGRATION
                     FORGE
```

with the standing distinction:

```text
BCP CONTROL SUBSTRATE
    generic coordination / leases / state / automation

BCP MIGRATION SYSTEM
    VIVIM → Ω archaeology / assay / mapping / verification

VIVIM
    legacy source mine / behavioral source

Ω
    destination architecture / runtime
```

Do not collapse these into one architecture. Do not re-litigate V1's settled
resolutions (CONFLICT-REGISTER.md C1–C10) unless new evidence contradicts them —
if it does, open a C14+ entry with the evidence, never silently rewrite.

---

# 2. CRITICAL RULE (UNCHANGED)

## CLEANUP ≠ DELETION

Do not delete something merely because it is old, obsolete-looking,
duplicated, unreferenced, abandoned, architecturally changed, superseded, or
**created by an interrupted agent run**. The Prompt-4 checkpoint (`a528ffd`)
is preserved work, not debris.

First determine whether it contains historical reasoning, evidence, decisions,
behavioral knowledge, migration provenance, implementation history, or forensic
findings. Prefer `git mv` + status markers over deletion. Deletion requires
evidence.

---

# 3. NON-GOALS (UPDATED — V1 list stands, plus)

Do NOT:

* resume, continue, or complete Prompt 4 (resumption needs an owner decision —
  see §30; your resumption verdict is a REPORT, not an execution);
* re-verify MIG-001/MIG-002 (VERIFIED; `verify_migration.py --all` is a
  regression check only);
* write the MIG-003 record/report (that is Prompt-4 resumption work);
* expand, shrink, rename, or move `sdk/` (D-404 frozen; outcome INTERNAL_ANVIL
  recorded in `docs/cleanup/SDK-ANVIL-ACCOUNTING.md`);
* create a second plugin scaffolder, extend `tooling/builder`, or touch
  `tooling/generate plugin` (quarantined pending owner decision);
* add/commit `compositions/*.json` specs (18-freeze; matrix path only);
* touch the `a528ffd` checkpoint contents except via the resumption decision;
* redesign Omega, BCP, or migration semantics; modify VIVIM; port code;
  change ratified decisions or rewrite their records.

---

# 4. REPOSITORY AREAS TO AUDIT (UPDATED)

V1 areas stand. Additionally audit everything new or changed since `becb920`:

```text
docs/cleanup/              (7 docs: REPORT, AUTHORITY-MAP, CONFLICT-REGISTER,
                            PROMPT-4-CHECKPOINT, SDK-ANVIL-ACCOUNTING,
                            PLUGIN-AUTHORING-PATH-AUDIT, PLUGIN-BUILDER-ARCHITECTURE)
docs/CURRENT-CONTEXT.md    (V1 patch: Prompt-4 + authoring pointers)
/AGENTS.md /BUILD_CONTEXT.md (cold-start chain)
/docs/migration/           (V2 additions? MIG-003 draft under bcp-speed/bcp/migration/)
omega-baseline/omega-final/docs/architecture/      (Prompt-4 analysis, now TRACKED via a528ffd)
omega-baseline/omega-final/examples/plugin-echo2/  (now TRACKED)
omega-baseline/omega-final/plugins/parser-claude-sse/   (now TRACKED)
omega-baseline/omega-final/plugins/parser-claude-sse-v2/ (now TRACKED)
omega-baseline/omega-final/bun.lock               (regenerated by install — must ride WITH plugin commits)
bcp-speed/bcp/migration/MIG-003-claude-sse-parser/ (DRAFT: assay/spec/mapping, NO record/report)
```

Still hands-off (verify untouched, do not classify further without owner):
`bcp-algos/`, `setupdocs.zip`.

---

# 5. FIRST PHASE — FREEZE AND DELTA INVENTORY (UPDATED)

Before changing anything:

1. Run `git status --short`, `git branch --show-current`, `git log -10 --oneline`,
   `git diff --stat`, `git reflog -10`.
2. Record HEAD. Confirm `becb920` (or its named successor) is the base.
3. Determine whether Prompt 4 RESUMED without authorization: new commits touching
   the `a528ffd` paths, new MIG-003 record/report, new compositions, gate
   changes. Any resumption activity is a §30 input, not something to extend.
4. Inventory ONLY the delta since `becb920` at file level
   (path/type/size/role/references). Full re-inventory is forbidden unless the
   delta exceeds ~50 files — cite V1's 4143-file audit instead.

---

# 6. DOCUMENT AUTHORITY ARCHAEOLOGY (REFERENCE V1 + DELTA ONLY)

Classifications and definitions are UNCHANGED:

```text
CURRENT / LAW / HISTORICAL / ARCHIVED / RAW-RESEARCH / UNKNOWN
```

Classify ONLY: files created/modified since `becb920`, plus any V1-UNKNOWN that
gained evidence. Default dispositions pre-established by V1 + checkpoint:

```text
docs/cleanup/* (7) ............ CURRENT (cleanup evidence)
PROMPT-4-CHECKPOINT.md ........ CURRENT (as checkpoint record; the WORK it
                                describes stays PROMPT-4-INTERRUPTED, never CURRENT)
echo2 / parser plugin code .... INTERRUPTED work: implemented-but-unratified;
                                NOT current authority until resumption gates pass
MIG-003 draft (3 files) ....... INCOMPLETE draft, not a record
```

---

# 7. AUTHORITY GRAPH (EXTEND, DO NOT REBUILD)

`docs/cleanup/AUTHORITY-MAP.md` stands. Add rows ONLY for:

```text
Prompt-4 checkpoint ......... docs/cleanup/PROMPT-4-CHECKPOINT.md (CURRENT record of INTERRUPTED work)
Builder architecture ........ docs/cleanup/PLUGIN-BUILDER-ARCHITECTURE.md (CURRENT explanation)
SDK status .................. docs/cleanup/SDK-ANVIL-ACCOUNTING.md → INTERNAL_ANVIL
Authoring audit ............. docs/cleanup/PLUGIN-AUTHORING-PATH-AUDIT.md
Resumption gate ............. CONFLICT-REGISTER.md C13 (OPEN, owner-owned)
```

Trace `OLD PATH vs CURRENT NATIVE PATH` (§34 of the convergence brief) for any
new authoring-adjacent material. If a new doc claims an authoring path, it
CONFLICTS until reconciled — open a C14+ entry.

---

# 8. SEARCH FOR CONTRADICTIONS (UPDATED)

V1's C1–C10 are SETTLED — regression-check only (spot-verify banners/paths
intact; do not re-argue). Active hunting focuses on:

* **C13 resumption preconditions**: were `omega:quick`, parser conformance
  runs, MIG-003 numbering, forge-path posture resolved, or are they still OPEN?
* **New number drift**: host LOC (1500/1500 flat), composition count (18),
  capability count (49), log events (105 at becb920), discoveries (27 at
  becb920). Any prose written since becb920 citing older numbers is a finding.
* **Duplicate-claim drift**: echo2/parser docs vs harvest docs vs
  `docs/migration/` — same behavior claimed authoritative in two places?
* **Verification-language drift**: any post-becb920 text calling Prompt-4 work
  done/verified/current/ratified (it is INTERRUPTED — §4 of the checkpoint).
* Standing checks (unchanged): terminology, Ollama-vs-Chrome (D-418/D-456),
  merging-vs-integrated, fixture-vs-live.

---

# 9. KNOWN AREAS — V1 RESOLUTIONS STAND, VERIFY + THREE NEW ITEMS

* A (orchestration history) — RESOLVED by banners. Verify banners intact.
* B (setupdocs) — RESOLVED (SUPERSEDED/HISTORICAL). Verify no new live
  references to setupdocs as instructions.
* C (migration strategy split) — RESOLVED. Verify `docs/migration/` vs Ω
  `docs/migration/` still correctly attributed after any additions.
* D (forge generations) — RESOLVED. Verify plus-file banner intact.
* E (active/archive pairs) — RESOLVED. Spot-check.
* F (raw conversations) — RESOLVED (archived). Spot-check.
* **G (NEW) — Prompt-4 checkpoint coherence**: does every `a528ffd` file have
  a checkpoint disposition? Has anything been built ON the checkpoint that
  assumes resumption?
* **H (NEW) — quarantine compliance**: has `tooling/generate plugin` gained
  features? Has any new scaffolding appeared? Has `sdk/` gained exports
  (anvil-surface is 45 — count them)?
* **I (NEW) — MIG-003 draft status**: still record-less? Any text treating the
  draft as a record?

---

# 10. CODE ARCHAEOLOGY (NARROWED)

No refactor. Verify the three tracked Prompt-4 code additions against their
checkpoint dispositions (KEEP-BUT-RECONCILE): no prod-code drift since `a528ffd`
(`git diff a528ffd -- <paths>` must be empty unless the resumption decision
covers it), no new import edges into them from product code, gate-relevant
counts noted (2 new `plugins/` dirs, 1 new `examples/` dir — genome/count
sensitivity is a §30 resumption precondition, NOT a cleanup action).
Dispositions KEEP/KEEP-BUT-MARK/ARCHIVE/QUARANTINE/REMOVE/INVESTIGATE and the
zero-call-site caution from V1 are UNCHANGED.

---

# 11. ACTIVE AGENT INSTRUCTION AUDIT (UPDATED)

`/AGENTS.md` now EXISTS and is the ranked chain — audit it first, then every
instruction source for WHO/WHEN/CURRENT/CONFLICT. New sources since V1:

```text
docs/cleanup/PLUGIN-BUILDER-ARCHITECTURE.md (agent path guidance — must agree with /AGENTS.md)
docs/cleanup/PROMPT-4-CHECKPOINT.md (must NOT read as instructions to resume)
omega-…/docs/architecture/OMEGA_REPLACEMENT_MODEL.md (unratified analysis — must NOT present as law)
```

Detect anything telling agents to: resume Prompt 4, treat INTERRUPTED work as
current, use `tooling/generate plugin` as canonical, treat `sdk/` as an
authoring API, hand-edit specs, or modify BCP state directly.

---

# 12. COLD-START PATH (VERIFY + EXTEND, DO NOT REBUILD)

`/AGENTS.md`, `/BUILD_CONTEXT.md`, `docs/CURRENT-CONTEXT.md` exist. Verify
coherence back-to-back; patch (append-only) ONLY for: post-becb920 truth
changes, broken pointers, or cold-start failures below. Extended question list
(V1's 13 stand — confirm each still answers — plus):

```text
14. What happened to Prompt 4?
15. What is the canonical plugin-authoring path?
16. Is sdk/ the developer SDK?
17. Which authoring mechanism is quarantined, and why?
```

Any answer requiring historical digging fails §25-equivalent and must be fixed
by pointer, not prose.

---

# 13. DO NOT CREATE A SECOND ARCHITECTURAL CONSTITUTION (UNCHANGED)

Point at ratified decisions, contracts, implementation, evidence. `docs/cleanup/`
answers "what/why/where" — never re-specifies Ω behavior. V2 adds NO new
cleanup-doc types beyond a V2 addendum file if needed; prefer appending to the
existing 7 docs over creating an 8th.

---

# 14. ARCHIVE STRUCTURE (VERIFY ONLY)

V1's `docs/archive/` taxonomy + READMEs stand. New moves allowed ONLY for
material created after becb920 that is already-stale (unlikely) — default: no
moves. Obey `git mv` + reference repair if any occur.

---

# 15. STATUS BANNERS (ADDITIVE ONLY)

V1's 18 banners stand — verify a sample still present. New banners ONLY for
genuinely new staleness discovered in the delta. Never banner the `a528ffd`
checkpoint files themselves (their checkpoint doc carries the status).

---

# 16. DUPLICATE DOCUMENT RULE (UNCHANGED + ONE WATCH)

V1 rules stand. Watch item: Prompt-4 analysis docs
(`docs/architecture/OMEGA_REPLACEMENT_MODEL.md`, `Omega_Replacement_Analysis.md`)
vs `docs/cleanup/PLUGIN-BUILDER-ARCHITECTURE.md` — overlapping questions,
different standing (unratified analysis vs cleanup explanation). If either is
quoted as law, open a conflict; do not merge (different purposes).

---

# 17. DECISION RECORD RULE (UNCHANGED)

Ratified Ω records are genealogy. Supersession = new D-record with explicit
`supersedes D-NNN`. V2 adds: the `generate plugin` retirement and any anvil
change each need their OWN future D-record — note the requirement, do not
draft the records (prompt scope ends at cleanup).

---

# 18. BCP STATE RULE (UPDATED COUNTS)

`bcp-speed/bcp/state/` remains control-plane truth (49 capabilities, 8 leases,
105 log events, 27 discoveries at becb920 — re-verify, report drift with cause:
tool writes are expected, hand-edits are findings). `migration/` vs control
substrate separation stands. No state redesign.

---

# 19. Ω CODE RULE (TIGHTENED)

Read/audit/classify/document — no redesign. Explicitly newly forbidden (beyond
V1): anything that would pre-empt the §30 resumption gates (running the
resumption test suites AS resumption, writing the MIG-003 record, editing the
checkpoint files' semantics). Verification-grade reads and test RUNS are
allowed as evidence (read-only execution); semantic edits are not.

---

# 20. REFERENCE INTEGRITY (DELTA ONLY)

Check references for post-becb920 files (checkpoint, 3 audit docs, CURRENT-
CONTEXT pointers). Re-run V1's old-path grep only if new moves occurred
(default: none). Pay attention to: prompt/ инструкция references to setupdocs
as live instructions, and any new doc pointing at `a528ffd` paths as current.

---

# 21. GENERATED / MACHINE-OWNED FILES (UNCHANGED + bun.lock NOTE)

V1 rules stand. Addition: `omega-…/bun.lock` regenerates via `bun install`
when workspace members change — the ONLY lawful state is "committed together
with the member change" (as in `a528ffd`). A lockfile diff WITHOUT its member
change (or vice versa) is a finding.

---

# 22. CLEANUP REPORTS (V2 ADDENDUM, NOT REWRITE)

Do NOT rewrite the three V1 reports. Produce:

```text
docs/cleanup/REPOSITORY-CLEANUP-REPORT-V2.md   (date, base becb920 → end SHA,
    delta files moved/modified/deleted/retained, ambiguities, refs fixed,
    unresolved questions, V1-regression verdict)
```

plus surgical appends: CONFLICT-REGISTER C14+ (only new/resolved conflicts —
C13 resolved or advanced with evidence), AUTHORITY-MAP delta rows (only if
authority actually changed). quoting V1's scorecard as baseline, not Bedrock.

---

# 23. CLEANUP SCORECARD (V2 = DELTA + REGRESSION)

Report V1-baseline regression (all V1 counts re-verified or drift explained)
PLUS delta counts (files audited since becb920, newly classified, moved,
modified, deleted (=0 expected), links repaired, new conflicts, closed
conflicts). Same category vocabulary as V1.

---

# 24. HARD SAFETY CHECKS BEFORE COMMIT (V1 FIVE + SIXTH)

V1 checks 1–5 UNCHANGED. Add check 6 — **no resumption smuggled in**: the diff
must contain zero of {MIG-003 record/report, composition spec adds, gate-count
changes presented as improvements, checkpoint semantics edits, new plugin dirs
beyond `a528ffd`}. Any such content → stop, report, await owner direction.

---

# 25. FINAL COLD-START TEST (V1 13 + 4 NEW — §12)

New agent reads ONLY `/AGENTS.md`, `/BUILD_CONTEXT.md`,
`docs/CURRENT-CONTEXT.md` (+ area authorities they name). Must answer V1's 13
plus 14–17 above from those pages alone.

---

# 26. REQUIRED FINAL REPORT (V1 A–F + RESUMPTION DELTA)

A–F as V1, plus: V1-regression verdict per area, C13 advancement (OPEN →
resolved/advanced with evidence and owner), quarantine-compliance verdict
(H), and an explicit resumption-delta statement: what, if anything, changed
about Prompt-4's SAFE_AFTER_RECONCILIATION preconditions (quick re-run,
parser conformance, MIG-003 numbering, forge-path posture).

---

# 27. COMMIT DISCIPLINE (UPDATED)

Focused commit(s), cleanup-only. Suggested message:

```text
cleanup-v2: verify V1 reconciliation held, reconcile post-becb920 delta
```

Preservation-style commits (like `a528ffd`) are FORBIDDEN in this task unless
newly interrupted work is discovered — and then only with the same
INTERRUPTED labeling, never as completion.

---

# 28. DEFINITION OF DONE (V1 15 + 4)

V1's fifteen conditions must still hold (re-verify, don't re-prove) PLUS:

```text
✓ delta since becb920 fully classified (or UNKNOWN with owner)
✓ C13 advanced or reaffirmed OPEN with fresh evidence
✓ quarantine compliance verified (generate-plugin, sdk surface = 45, no new scaffolders)
✓ resumption preconditions re-checked and reported (not executed)
```

---

# 29. FINAL PRINCIPLE (UNCHANGED)

Not smaller. Not resumed. **Harder to misunderstand**: CURRENT obvious, LAW
explicit, HISTORY preserved, RAW RESEARCH quiet, UNKNOWN visible,
CONTRADICTIONS named, FUTURE ARCHITECTURE never presented as present reality.

---

# 30. (NEW) PROMPT-4 RESUMPTION PRECONDITIONS — VERIFY, DO NOT EXECUTE

Resumption status at V2 assignment: SAFE_AFTER_RECONCILIATION with four
preconditions. Your job is to REPORT each as MET/UNMET (evidence-cited):

```text
1. omega:quick re-run green WITH the 3 new plugin/example dirs present
   (genome/count sensitivity) .................... MET / UNMET
2. runConformance green on plugins/parser-claude-sse*/ (staged→verified→active)
   ................................................ MET / UNMET
3. MIG-003 numbering decided (experiment=MIG-003 → Gemini=MIG-004, or owner override)
   ................................................ DECIDED / UNDECIDED
4. Future scaffolding posture: forge.author path named for any new plugin work
   ................................................ CONFIRMED / UNCONFIRMED
```

Running these gates as EVIDENCE is allowed (read-only execution, no semantic
edits). Acting on failures (fixing code to make them pass) is RESUMPTION —
stop and report. All four MET still does NOT authorize resuming; it authorizes
the owner to decide on a clean board.

---

# 31. (NEW) V1→V2 CHANGE LOG (why this update exists)

```text
a. Baseline moved 18242a3 → becb920 (two cleanups + one preservation commit landed).
b. SDK question answered: INTERNAL_ANVIL (was open audit) — V2 verifies, not re-audits.
c. Authoring converged: canonical path named; generate-plugin quarantined (was three-way ambiguity).
d. Prompt-4 state known: INTERRUPTED with checkpoint (was unknown uncommitted work).
e. MIG-003 draft exists without record (was "next: Gemini") — numbering tension explicit.
f. Cold-start chain exists (was to-be-created) — V2 patches, not builds.
g. bun.lock/workspace discipline recorded (new failure mode observed).
h. Counters refreshed (49 caps / 105 events / 27 discoveries / 18 specs / 1500 LOC / 45 exports).
i. Sixth safety check added (no smuggled resumption) — the new dominant risk.
j. Resumption preconditions made explicit and verifiable without executing.
```
