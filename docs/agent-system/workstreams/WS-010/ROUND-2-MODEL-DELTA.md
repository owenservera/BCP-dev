# WS-010 Round 2 — Model Delta (additive corrections only)

> **Classification: DERIVED — PROPOSED (WS-010 research, not authority)**
> Round-1 MODEL-DELTA stands unchanged. This file records ONLY what Round 2
> adds. The V0 blueprint is NOT modified: no falsifier demonstrated
> impossibility or contradiction at the semantic level.

## R2-A — adopted into the model (binding for Round 3 prep)

1. **N/A vs UNKNOWN discipline** (§Q11, X-APPL): `N/A` only where the
   kind×axis applicability table allows; otherwise `UNKNOWN`. Two visibly
   distinct nulls.
2. **Vocabulary ≠ population** (§N-VOCAB): empty-extension terms render
   definition + honest empty set; no synonym backfill (`Task` stays out).
3. **mtime is not a source** (Round-1 F-02 carried over): freshness derives
   from tips/banners/registries only.
4. **Rejection is preservation** (§Y-CONF): considered-and-rejected
   inferences persist as rejected-candidates for audit projections.
5. **Five-layer separation** (PRESENTATION-VS-SEMANTICS §S-LAYERS):
   source objects / source assertions / derived assertions / presentation
   grouping / visual projection. Layer confusion is now a named review
   failure.
6. **Grouping-not-containment marking** (§S-GROUP): nested visuals without
   a Y-matrix `contains` row carry the marker; zoom asserts nothing.
7. **Name-stripping card test** (Z-NAME): card templates pass only if the
   node stays uniquely addressable with the display name removed.
8. **Reference-survives-referent** (§Q4/Z7): deleted objects keep
   preserved references with ABSENT targets, never ghost renders or
   erasures.

## R2-B — open feasibility items (no semantics at risk; owner-tracked)

B1. Rebuild-receipt schema (inputs + rule versions + source tips).
B2. Versioned visual-mapping table over named axes (X-NORM).
B3. Work-Map family → axis-value binding table.
B4. Agent-Map multi-scope hop rendering.
B5. Explicit volume sampling/selection rules for post-thin-slice scale.
B6. Name-stripping test harness for card templates.

## R2-C — blueprint impact: NONE

No V0-BLUEPRINT / VIEW-SPEC / DESIGN-DECISIONS edit proposed. If a future
round finds a semantic contradiction, it will be recorded here in the
form (blueprint-section × contradicting-evidence × proposed-correction)
before any edit — silent rewrites remain forbidden.
