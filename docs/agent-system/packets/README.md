# packets/ — Compact Extracted Working Knowledge

> **Classification: convention (DERIVED — CURRENT); each packet carries its own banner + FRESHNESS**

File: `packets/PKT-NNN-<slug>.md`, revisions `PKT-NNN-<slug>-v2.md` (old kept,
marked SUPERSEDED with forward link — never silent in-place rewrite).

## Required sections

```text
packet id / source transcript(s) + source ranges / workstream / classification
facts (each with: claim, source kind, source id, source location, hash where
  practical, extraction session, current status)
discovered concepts
proposals
contradictions
unknowns
reasoning lineage (why each idea appeared, what assumption it corrected, what
  evidence changed the view, what repo evidence supports it, what remains
  hypothetical, what was explicitly rejected)
recommended reads
freshness + supersedes/superseded-by links
```

Packets are DERIVED and may be stale or superseded. They are never
constitutional authority.
