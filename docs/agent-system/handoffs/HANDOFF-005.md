# HANDOFF-005 — SEALED multi-agent independence closer (launch-ready)

> **Classification: DERIVED — CURRENT**
> **Seal:** this handoff is a self-contained launch package. The owner launches
> ANY fresh agent/thread by giving it exactly this file plus the repository at
> the base commit — nothing else, no cover note with hidden context.

- **HANDOFF ID:** HANDOFF-005
- **SOURCE AGENT:** IMPL-02 (closer author; must have no further contact with the closer run)
- **SOURCE SESSION:** `20260923-224219-dir-002-stage-sealed-closers-for-the-two` (Ω ledger)
- **TARGET AGENT:** IMPL-03 — reserved designation for the independent closer
  (coordinator to register on launch; any distinct identity + distinct thread qualifies)
- **WORKSTREAM:** WS-001 (P1-01) · **BASE COMMIT:** `b5cdb24` · **SEALED:** 2026-09-24

## BOOTSTRAP MANIFEST (exact — the launcher gives precisely this, no more)

PERMITTED — this file, plus from the repository only:

```text
docs/agent-system/CHATGPT-BOOT.md
docs/agent-system/CURRENT.md
docs/agent-system/handoffs/HANDOFF-004.md
the repository itself at the base commit
```

FORBIDDEN to the closer run (launcher must not provide; closer must refuse if offered):

```text
the originating owner/ChatGPT conversation or any transcript of it
DIR-001 reasoning, drill deliberation, or PKT-002 reasoning
any cover note, summary, or verbal brief beyond this file
contact with IMPL-02 / TEST-01 / DOC-01 drill participants about the task
```

## MISSION (meaningful continuation, not acknowledgment)

1. Orient: identify WS-001/P1-01's current state, the P1-01 verdict, and the
   two open proof closers — from the permitted material only.
2. Verify the D-DOG-01 fix end-to-end: walk every link in the WS-001 evidence
   chain (charter transcript → PKT-001 → PKT-002 → HANDOFF-001 → HANDOFF-004 →
   proof-table + QA items → directives). Report each link ALIVE or ROTTEN with
   file + line evidence. A rotten link is a finding, not a failure of this run.
3. Independent cold-start audit: starting from CHATGPT-BOOT alone, judge
   whether a fresh reader reaches the two open closers without reconstruction.
   Record the exact path taken and any gap.
4. Deposit: one findings packet (next free PKT id — check `packets/` first) + one
   handoff (next free HANDOFF id — check `handoffs/` first; HANDOFF-006 is
   already taken by the DIR-002 closing handoff) to COORD-01 + one outbox item
   under your own agent id.

## RECORDING OBLIGATIONS (load-bearing for the proof)

- Record exactly what you were given at bootstrap (files + commit + launcher words).
- In every finding, distinguish INHERITED (taken from HANDOFF-004/CURRENT on
  trust, with pointer) from ESTABLISHED (verified by your own inspection, with
  evidence).
- If you discover you received forbidden context, stop, record the breach, and
  hand back — a breached run proves nothing.

## FILES INSPECTED

- (To be filled by the closer run — that list is itself evidence.)

## FACTS ESTABLISHED / DISCOVERED / CONTRADICTIONS / UNKNOWN

- (To be filled by the closer run.)

## PROVEN RUBRIC (coordinator applies this, not vibes)

F-AGENT-MULTI-AGENT flips PARTIAL → PROVEN iff ALL hold:

- [ ] Closer identity + thread distinct from all DIR-001 drill participants (attested).
- [ ] Bootstrap record matches this manifest exactly (no forbidden context).
- [ ] Continuation is meaningful: link audit complete per-link + cold-start audit with path.
- [ ] Own packet + handoff + outbox item deposited through protocol.
- [ ] Inherited-vs-established separation visible in every finding.
- [ ] No contact with drill participants about the task during the run.

Any unchecked box → verdict stays PARTIAL with the box named. A breached seal → run discarded, closer re-staged.

## NEXT ACTION (for the closer run)

Execute mission → deposit → HANDOFF to COORD-01 requesting the rubric ruling.

## CONTEXT BUDGET RECOMMENDATION

This file + the four permitted reads. Nothing else exists for this run.
