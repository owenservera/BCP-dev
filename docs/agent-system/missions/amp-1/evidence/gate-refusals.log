# Self-test 1 — gate refusals (2026-09-24, branch mission/amp-1)

> **Classification: DERIVED — CURRENT**

All probes ran against the live L1 hooks (`core.hooksPath =
agent-tools/hooks`) and `agent_lint.py`. Failing commits leave no history;
tampered files were restored (`git status` clean after each probe).
Verdict: 5/5 refused or behaved as specified.

## T1 — mission-branch commit without STATE touch (live hook refusal)

Staged `docs/agent-system/missions/amp-1/SCRATCH.tmp`, ran
`git commit -m "probe: commit without STATE touch"`.
- pre-commit hook: `agent_lint: GREEN` (BCP validate 0 errors, staged checks pass)
- commit-msg hook: `REFUSED: mission-branch commit touches no STATE.md and
  carries no 'State-Unchanged: <reason>' trailer (branch mission/amp-1)`
- Commit aborted; worktree restored. PASS (refused with precise reason).

## T2 — State-Unchanged trailer excuses the STATE touch (direct call)

Staged scratch file, message file containing
`State-Unchanged: probe of trailer path`:
- `agent_lint: GREEN`. PASS (trailer path works, no commit made).

## T3 — Decision trailer without a new insight (direct call)

Staged scratch file, message containing `Decision: no-such-slug`:
- `REFUSED: mission-branch commit touches no STATE.md and carries no
  'State-Unchanged: <reason>' trailer (branch mission/amp-1)`
- `REFUSED: commit carries 'Decision: no-such-slug' but adds no matching
  file under context/insights/`
- PASS (refused with precise reasons). The positive case
  (Decision + new insight file) is covered by unit test
  `test_decision_with_insight_passes`.

## T4 — edit to a committed insight (staged, then restored)

Appended `tamper line` to
`docs/agent-system/context/insights/20260924-193000-allowlist-ignore.md`,
staged, ran `agent_lint.py --pre-commit`:
- `REFUSED: immutable path modified:
  docs/agent-system/context/insights/20260924-193000-allowlist-ignore.md
  (insights/transcripts/packets are append-only; supersede, never edit)`
- Restored via `git checkout --`; status clean. PASS.

## T5 — edit to a committed transcript (staged, then restored)

Appended `tamper line` to
`docs/agent-system/transcripts/2026-09-23/CHAT-2026-09-23-cooperative-agent-context.md`,
staged, ran `agent_lint.py --pre-commit`:
- `REFUSED: immutable path modified:
  docs/agent-system/transcripts/2026-09-23/CHAT-2026-09-23-cooperative-agent-context.md
  (insights/transcripts/packets are append-only; supersede, never edit)`
- Restored via `git checkout --`; status clean. PASS.

## T6 — write outside the allowlist (staged, then removed)

Staged repo-root `PROBE-OUTSIDE.txt`, ran `agent_lint.py --pre-commit`:
- `REFUSED: path outside charter allowlist for amp-1: PROBE-OUTSIDE.txt`
- Unstaged and deleted; status clean. PASS.

## T7 — clean-tree claim over a dirty tree

See `recovery.log` §R5 (STATE transiently claimed `tree clean` while the
tracked tree was dirty; lint refused; STATE restored). PASS.
