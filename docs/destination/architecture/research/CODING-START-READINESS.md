# Coding Start Readiness

> Classification: DERIVED — STEWARD CONVERGENCE / FRESH TAKEOVER PASS
> Date: 2026-09-25
> Current main re-grounding: 13fc6c08d4a0583d620bd5baffc259cfb8e6d044
> Authority: Ω ratified law remains authoritative; this document is a derived coding boundary.
> Execution note: the dedicated subagent facility is not exposed in this session, so the required Round 2 convergence was executed directly by the task owner using the exact Round 2 prompt and its required inputs. No subagent run is claimed.

## 1. Fresh decision

The fresh two-round operation supports:

**CODING STATUS: GO**

This is a new convergence decision, not a ratification of the prior readiness document.

The critical new clarification from fresh Round 1 is that the first composition's “research capability” must be an explicit ordinary first-party plugin. It is not hidden infrastructure and does not require a K0 expansion.

The repository therefore has enough evidence to build the factory experiment now.

## 2. Fresh Round 1 execution proof

The four bounded investigations were re-executed against current repository truth, with the prior artifacts treated as historical inputs.

| Investigation | Fresh commit | Finding |
|---|---|---|
| Factory Boundary | 6b2a8723eb463ae98b25abb3bc24b0b3adb40cea | READY |
| Reference Legos | 532326007fcc82b294461403c74ed7de4c0ad9a8 | READY |
| First Composition | d87f242e30220140a591ad9436e3ffb2fc844be6 | READY |
| Factory UX | b152c1652dba6a9329cd012d664385b1ee884e20 | READY |

Fresh outputs:

- `docs/destination/architecture/research/FACTORY-BOUNDARY-BASELINE.md`
- `docs/destination/architecture/research/REFERENCE-LEGOS-BASELINE.md`
- `docs/destination/architecture/research/FIRST-COMPOSITION-BASELINE.md`
- `docs/destination/architecture/research/FACTORY-UX-INVARIANTS.md`

The prior four commits remain historical lineage:
65f108e, f592a55, d98e315, e53eb60.

## 3. What the fresh pass re-grounded

The current Ω law still states:

- B1: executable entry must be covered by signed Recipe/manifest/content integrity;
- B2: plugin isolation and Port transport;
- B3: host-side capability-token enforcement and generation fencing;
- B4: fail-closed verification and atomic recovery;
- B5: the µhost is frozen at 1500 LOC and cannot grow without equal-or-greater removal.

The current contract sources still establish:

- Recipe as the composition grantor: `omega-baseline/omega-final/contracts/src/recipe.ts`;
- one manifest vocabulary for plugins: `omega-baseline/omega-final/contracts/src/manifest.ts`;
- canonical intent persistence: `omega-baseline/omega-final/contracts/src/intent.ts`;
- law, vault, run, mind and provider-browser as explicit plugin contributions;
- Forge as proposal/creation machinery that does not receive special authority.

Current System Intelligence still distinguishes:

- verified runtime substrate from partial product maturity;
- provider-browser code from live owner-machine proof;
- Work/evidence mechanisms from a complete product Work lifecycle;
- mind from an authoritative world store;
- Product Instance design from assembled product lifecycle.

## 4. Factory boundary

The smallest credible factory is:

```
K0 Ω
  admission / integrity
  isolation / Port transport
  capability egress / fencing
  activation / recovery
  cryptographic / canonical primitives
  generic runtime lifecycle
        ↓
K1
  Recipe / Manifest / Port / references / outcome vocabulary
        ↓
System plugins
        ↕
Extension plugins
        ↓
Compositions
        ↓
Product Instance
```

The host remains constitutional infrastructure.

Product semantics, provider semantics, world semantics, Work semantics, surface semantics and creation semantics remain outside K0 unless a future invariant proves otherwise.

A new product capability must be able to enter through the same governed boundary. A special “first-party” host path would be a factory falsifier.

## 5. Building language

No new ontology is required.

The first implementation uses:

`Capability / Plugin / Contract / Composition / Object / Surface / Realization / Authority / Work / Evidence / Forge / Evolution / Product Instance`

The operational construction language is:

`Recipe + Manifest + Port + contribution + grant + composition + lifecycle`

The key distinctions remain:

- capability ≠ realization;
- realization ≠ authority;
- evidence ≠ authority;
- work ≠ execution mechanism;
- representation ≠ canonical truth;
- candidate ≠ active realization.

## 6. K0 / K1 / plugin / tooling placement

### K0

Keep only constitutional, domain-neutral, non-bypassable mechanisms:

- admission/integrity;
- isolation/transport;
- capability egress enforcement;
- revocation/generation fencing;
- atomic activation/recovery;
- minimum cryptographic/canonical primitives;
- minimal platform/runtime seam.

### K1

Shared interoperability vocabulary:

- Recipe / Manifest;
- Port;
- capability/plugin references;
- intent/work/object/evidence references;
- lifecycle and refusal/outcome shapes.

### System plugins

First-party VIVIM semantics:

- `vivim.law`;
- `vivim.vault`;
- `vivim.run`;
- `vivim.mind`;
- intent/NLCL realization;
- provider/account/session/browser realizations;
- the first thin research capability;
- Forge and product surfaces as those slices are implemented.

### Extension plugins

Third-party and user-created contributions through the same governed boundary.

### Tooling

Generators, falsifiers, audits, CI, migration/research utilities and other out-of-runtime machinery.

## 7. Reference pieces

The fresh Reference Legos pass resolves the first teaching set as:

1. `vivim.law` — Authority.
2. `vivim.vault` — durable truth and Evidence.
3. `vivim.run` — Work/execution boundary.
4. `vivim.mind` — derived World/context lens.
5. existing NLCL/intent path — Address/Intent.
6. `provider.browser` — external Realization.
7. thin first-party research capability plugin — first useful domain behavior.

`forge.author` is not required for the first external-outcome composition. It becomes the reference for the next factory loop: user creation and evolution.

Legacy harvest remains behavioral:

- ChromeGovernor/profile isolation;
- ProviderMux routing ideas;
- semantic grounding;
- guided provider probing;
- stream alignment;
- parser/selector repair;
- provider health;
- replay fixtures;
- adaptive workspace/canvas/conversation behavior.

These are to be re-expressed behind current Ω boundaries, not copied structurally.

## 8. First meaningful composition

### Research → Evidence → World

The first useful user outcome is:

> research a topic for a project and save the result into the user's environment.

The fresh trace is:

```
User
→ factory surface/composition
→ Address / Intent
→ bounded Context / World
→ research capability plugin
→ provider.browser realization
→ vivim.law authority
→ vivim.run Work where consequential
→ browser execution
→ vivim.vault Evidence + result
→ vivim.mind World projection
→ Product Instance continuity
```

The research capability is intentionally a normal system plugin. It may initially use an existing `message.send@1` contract/realization family rather than introducing a new host primitive.

The first provider/account choice is bounded to one configured realization/account. General routing policy is not part of the first proof.

## 9. Ω reuse, extension and replacement

### Reuse

- Recipe/Manifest admission;
- µhost/Port;
- host-side capability verification;
- law gate;
- vault durability/revision/evidence;
- canonical intent;
- ProviderRealization vocabulary;
- fixture-first provider-browser;
- mind as derived lens;
- Forge promotion machinery.

### Extend

- Product Instance activation/continuity wrapper;
- first research capability plugin;
- canonical Work/product continuity join;
- bounded composition/surface entry;
- close/reopen lifecycle proof.

### Replace only at explicit seams

Provider realizations, surface implementations, storage implementations behind contracts and other replaceable contributions remain plugin/evolution seams.

## 10. Blocking versus non-blocking unknowns

### Architecture-blocking unknowns

None currently supported by the fresh evidence.

No known unresolved fact presently requires redesign of the K0/K1/plugin factory boundary.

### Implementation/proof gates

1. **B1 executable-entry confinement** — fix inside existing admission boundary; respect B5 remove-to-add.
2. **Generic bootstrap / zero-plugin semantics** — prove the generic bootstrap seam without moving product semantics into K0.
3. **State / Graph / Grant / Generation reduction** — reduce only what the first build actually requires.
4. **First-party / extension symmetry** — prove both shapes through the same Recipe/Manifest/Port path.
5. **Work replacement continuity** — prevent silent reinterpretation of authorized active Work.
6. **Product Instance activation** — candidate → verified → compatible → active, preserving the previous activation until readiness.
7. **Live provider proof** — owner-machine Chrome proof remains distinct from fixture/code evidence.

These are build-time falsifiers and integration gates, not permission for another broad archaeology cycle.

## 11. Exact E2E proof path

The first executable proof should:

1. initialize/open one Product Instance;
2. load an admissible composition containing the reference substrate and research capability;
3. address a project/topic through the existing intent path;
4. assemble bounded context;
5. resolve the research capability;
6. select one declared browser realization/account;
7. pass the law/consent boundary;
8. create durable Work when the action is consequential;
9. execute through the browser realization;
10. persist result and Evidence in the vault;
11. read it through the World projection;
12. close the instance;
13. reopen the same instance;
14. verify the same canonical result/Evidence and Product Instance identity remain addressable.

Acceptance:

```
same instance identity
same canonical evidence
same world basis
same governed attribution
runtime process may differ
```

The live-provider step is not implied by fixture success.

## 12. Ordinary-user creation boundary

The end-state factory path is:

```
discover gap
→ describe desired piece/set
→ construct candidate
→ inspect dependencies/effects/authority
→ test/falsify
→ promote
→ use as ordinary piece/set
→ inspect
→ replace/evolve/remove
```

The Personal Agent may make this intelligible, but it is not a privileged authority.

The first build must avoid architectural choices that would later require a developer-only creation subsystem.

## 13. Explicit cut line

Do not wait for:

- all 125 responsibilities;
- full World breadth;
- complete Canvas/product shell;
- all providers/accounts/routing;
- autonomous evolution;
- complete attention/notification;
- multi-device continuity;
- extension marketplace/distribution;
- finished native Windows shell;
- complete export/reconstruction product journey;
- full provider healing;
- another ontology or architecture portfolio.

These are later build/research surfaces.

## 14. Precise stop condition

Preparation stops because the following questions are now sufficiently answerable from repository evidence:

1. What is the factory? — governed Ω substrate plus K1 interoperability and replaceable plugins.
2. What is the language? — Recipe/Manifest/Port plus the existing capability/contract vocabulary.
3. What proves the language? — law, vault, run, mind, intent, browser realization and the first research capability.
4. What proves interoperability? — Research → Evidence → World.
5. What remains Core? — only the non-bypassable generic substrate.
6. What proves continuity? — close/reopen against the same Product Instance and canonical Evidence.
7. What would falsify the factory? — host exceptions, bypassed authority, second admission path, or inability to replace pieces without changing canonical identity.

The next meaningful information is therefore produced by code and falsifiers.

## 15. First build direction

```
1. harden K0 admission/bootstrap falsifiers
2. implement the Product Instance activation/continuity seam
3. add the thin research system plugin
4. compose intent → capability → realization → authority → Work
5. persist Evidence and World result
6. run close/reopen proof
7. expand factory creator/evolution UX only after the nucleus works
```

Every substantive implementation should record:

`BoundaryClass / SemanticOwner / WhyNotCore / Dependencies / AuthorityBoundary / ReplacementSeam / EvolutionClass / Falsifier`

## 16. Final convergence

This fresh pass does not claim VIVIM is complete or productized.

It establishes only that further broad architecture preparation currently has lower information value than implementing the smallest compositional factory experiment and observing where reality falsifies it.

CODING STATUS: GO