
# Open Frontier

> Classification: UNRESOLVED — intentionally preserved.

## Identity
1. Exact source-identity minimum for each provider class.
2. Whether account scope must always be encoded in source identity.
3. How provider migrations preserve external identity.
4. Whether a user can explicitly force two source identities to the same local object.

## Relationships
5. Final predicate vocabulary.
6. Relation fingerprint and duplicate suppression.
7. Conflict-resolution rules by predicate family.
8. Temporal relationship semantics.

## Content
9. Binary/large-content CAS implementation.
10. Inline-vs-reference thresholds.
11. External file mutation detection.
12. Content garbage collection coupling to semantic lifecycles.

## Lifecycle
13. Retention semantics for deleted object history.
14. Whether archived state may affect relation traversal.
15. Cross-object restore dependencies.

## Export
16. Atomicity of whole-world export across independently retained namespaces.
17. Existing-world merge/import protocol.
18. Identity remapping when restoring alongside an existing world.
19. External reconnect semantics.

## Projection
20. Which projections must persist versus remain fully rebuildable.
21. Basis-digest format for world/self-knowledge projections.
22. Workspace merge implementation details.
23. Surface-cache invalidation.

## Product
24. Empty-world first run.
25. User controls for source-identity collision.
26. Human presentation of contested relationships.
27. How a Space is created and deleted in ordinary UX.

These remain UNRESOLVED until an experiment, existing ratified law, or explicit product decision closes them.
