# Launch Prompt — VIVIM Canonical World / Object Core

Repository: https://github.com/owenservera/BCP-dev

Clone the repository from current main.

This is one focused deep research/design task:

> Determine and design the canonical VIVIM World/Object substrate that everything else can safely build upon.

Before beginning, read:
1. docs/destination/NORTH-STAR.md
2. docs/destination/FOUNDATIONAL-PRINCIPLES.md
3. docs/destination/CONCEPTUAL-MODEL.md
4. docs/destination/DESTINATION-MASTER-MAP.md
5. docs/destination/RECONCILIATION-MAP.md
6. docs/destination/DATA-MEMORY-CONTEXT-RECONCILIATION.md
7. docs/destination/agentic-core if present
8. latest System Intelligence Pass 1–3 artifacts
9. omega-baseline/omega-final storage/vault/evidence/object-adjacent code
10. Legacy conversation/content/workspace/file/document/artifact code and schemas.

Do not restart broad archaeology. Do not redesign Ω law, evidence, or plugin runtime.

Your task is to resolve semantic boundaries for:
Object, World, Message, Conversation, Document, File, Artifact, Project, Workspace, Space, Relationship, Revision, Source Identity, Projection, Alias, Tombstone, Export/Restore.

Determine what is canonical, what is derived, what is source-backed, and what is projection-only.

Pay special attention to identity collision, revision history, relation authority, source identity, external imports, content storage, delete/archive, restore and addressing.

Then design and execute local falsifiers. Browser and AI are not required.

The decisive test is the genuinely new object test: introduce an object type absent from the existing world and make it traverse the full lifecycle without creating duplicate storage truth or bespoke infrastructure.

No production implementation during this research phase.

Use labels:
OBSERVED, EVIDENCE-SUPPORTED, DESIGN-CANDIDATE, EXPERIMENT-REQUIRED, UNRESOLVED, REJECTED, PROMOTION-CANDIDATE.

Finish with the smallest VIVIM-native world/object model that can support future Work, agentic automation, provider results, self-knowledge and surfaces without being redesigned.


## Mandatory repository landing requirement

The research task is not complete while deliverables exist only in the agent workspace, chat, or a downloadable bundle.

Before declaring completion you MUST:
1. Write all required research/design deliverables into `docs/destination/world-object-core/`.
2. Include the required synthesis, evidence, indexes/experiment results and supporting artifacts defined by this workspace.
3. Update `STATE.md` with final status and exact commit SHA.
4. Commit the complete research package to this research branch.
5. Return the commit SHA and exact paths of key deliverables.
6. Keep/update the GitHub PR so the work is reviewable.

A ZIP is optional/secondary. The committed repository files are the canonical research landing place.

If literal local cloning is unavailable, use the accessible repository interface against the pinned source and still satisfy the GitHub commit/PR requirement. Do not stop at an external bundle.
