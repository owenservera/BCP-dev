# Launch Prompt — VIVIM Self-Knowledge / Freshness Core

Repository: https://github.com/owenservera/BCP-dev

Clone current main.

This is ONE focused deep research/design task:

> Design the smallest VIVIM-native self-knowledge system that can prove whether a derived view still matches its canonical sources.

Read destination docs, latest System Intelligence Pass 1–3, Ω mind/self-description mechanisms, vault/evidence/revision code, plugin/contract/manifest/version mechanisms, and canonical world-object work if present.

Do not create another ontology, provenance model, authority system or mandatory global invalidation bus without evidence.

Investigate:
DerivedView, basisRefs, basisDigest, dependencyVersions, computedAt, freshness, conflicts, derivationRef, invalidation, recomputation, external observation TTL.

Run local falsifiers:
- canonical revision change;
- contract/version change;
- plugin manifest change;
- policy change;
- source disappearance;
- contradictory source;
- restart of persisted derived view.

Prove that stale knowledge cannot silently appear current.

Keep self-knowledge non-authoritative: it may describe, compare, flag and propose; it cannot grant authority.

No browser or AI provider is required.
No production implementation during research.