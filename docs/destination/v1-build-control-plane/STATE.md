# V1 Build Control Plane — State

> Classification: DERIVED — PROPOSED / LIVE CONTROL STATE
> Current truth must be updated after substantive build work.

## Baseline
- Control-plane branch: product-vision/v1-build-control-plane
- Pass 3 baseline: 0121570c005112eb8875e9b8a6f484cc732d4e62
- Production implementation from this scaffold: NONE
- Live experiments E1–E8: UNRUN
- Current mode: DESIGN → EXPERIMENT

## Current gate
G0 — Protect Ω foundation

Then D1 — Account / Session / Browser Resource / Routing followed by E1–E4.

The first implementation target is not “Account” as an isolated feature. It is the smallest attributable external-effect chain:

one Account → one Session → one Browser Resource → one Realization → one governed effect → one Result → one Evidence chain

## Active constraints
- Do not redesign proven Ω foundation unless evidence requires it.
- Do not generalize browser semantics before E1–E4.
- Do not build a universal semantic object type.
- Do not copy the Legacy class hierarchy.
- Do not let routing become a hidden authority layer.
- Do not claim provider generality before E5.
- Do not claim durable background continuity before E7.
- Do not claim current self-knowledge without basis-aware freshness.

## State transition
CHARACTERIZED → DESIGNED → EXPERIMENTAL → PROVEN/REJECTED → IMPLEMENTED → INTEGRATED

A failed experiment returns the affected candidate to DESIGN or REJECTED; it does not silently become implementation debt.

## Update protocol
After each substantive slice record:
- what changed;
- evidence produced;
- gate affected;
- candidate promoted/narrowed/rejected;
- new dependency revealed;
- product journey unlocked;
- next smallest slice.

## Immediate next actions
1. Freeze the D1 candidate contract; do not pretend unresolved physical semantics are settled.
2. Prepare E1–E4 against the existing provider-browser leg.
3. Preserve an explicit evidence boundary between locator, resource identity, account identity, and provider identity; close D1 from experiment results.
