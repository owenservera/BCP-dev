# Launch Prompt — VIVIM Legacy Deterministic Automation Harvest

Repository: https://github.com/owenservera/BCP-dev

Clone current main.

This is ONE focused research/harvest task:

> Mine Legacy VIVIM for behavioral mechanisms that should inform the deterministic-first VIVIM product and distinguish reusable semantics from historical implementation coupling.

Read destination docs and latest System Intelligence first. Then inspect:
- source-atlas/02-GLOSSARY.md;
- source-atlas/L3-file-catalog/engines.md;
- source-atlas/L4-contracts/data-dictionary.md;
- ActionPlan/compiler/bridge;
- Plan Validation Gate;
- autonomous-types/planner/execution/replay;
- workflow-engine/compiler;
- automation scheduler/stores;
- retry queue;
- objective/outcome;
- execution policy/memoization;
- background machinery;
- conversation/message/history;
- workspace/project/canvas;
- export/recovery.

Do NOT copy the Legacy architecture, ORM proliferation or monolithic engine hierarchy.

For each finding trace:
Legacy behavior → semantic invariant → destination concept → evidence → preserve/rebuild/reference/experiment/reject.

Browser and AI providers are not required for the first pass.

No production code during research.

End with a compact harvest map that downstream implementation agents can use without opening the entire Legacy tree.