# Hidden Dependency Map

## Runtime

Recipe → grants/contracts → ports → plugin handlers → vault/law/credential state.

Configuration → blueprint paths / fence configuration → runtime behavior.

Provider session → debugPort → Chrome process/page → external provider state.

Discovery graph → candidates → mapping → verification → realization current state + audit history.

## Product

Account identity → session ownership → browser target → external effect → evidence.

Vault evidence → mind projection → world relationships → surface/workspace.

Run/director/watch → background state → evidence → return/attention.

## New critical observation

Pass 1 represented these mostly as conceptual atoms. Pass 2 identifies the actual runtime/data crossings and therefore exposes more precise build/proof seams.
