# Contradictions / tensions

- Legacy documentation treats ChromeGovernor exclusivity as a hard invariant; current BCP provider-browser deliberately uses a plugin-local CDP adapter. These cannot both be current architecture at once; the split is lineage, not simultaneous law.
- Legacy's API/local-model surface conflicts with current Chrome-only v1 sequencing and is therefore historical harvest evidence for that path.
