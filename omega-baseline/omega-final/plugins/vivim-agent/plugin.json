{
  "manifestVersion": "1",
  "id": "vivim.agent",
  "version": "0.1.0",
  "description": "Ω Control Plane v0 (D-309) + B1a execution (D-327): agent identity, lineage, and behavioral contracts as vault data. Spawns validate authority via the vendored scope algebra (subset of the parent's recorded scope, or of the composition grants for root spawns), never by minting host tokens. Forbidden actions register into vivim.law before the identity exists. agent.exec@1 admits one call per invocation (contract active + version-pinned + scope-covered + PROMOTED realization + law pre-check for the agent principal) and ledgers every attempt to ns agent id exec:<causationId> — refused-and-ledgered is legitimate, never silently retried. All expected-but-negative results return Outcome<T>; throws are malformed-payload/failed-port only (DEGRADED).",
  "entry": "src/index.ts",
  "publisher": { "keyId": "", "signature": "" },
  "contributions": {
    "engine": [
      { "kind": "engine", "id": "agency.execute", "version": "1", "doc": "execute the fixed Phase-1 message.send@1 capability under an explicit human/session principal + live consent + D-452 invocation frame; every success/refusal writes one governed event to ns agency; no standing or delegation." },\n      { "kind": "engine", "id": "agent.spawn", "version": "1", "doc": "spawn {parentId?, behaviorContractId, requestedScope, task?} → Outcome<{identity, scope, rev}> — behavior contract must be active; requested scope must sit inside the parent's recorded scope (or the composition grants for root spawns) and cover the contract's requiredCapabilities; forbiddenActions register into law before the identity appends." },
      { "kind": "engine", "id": "agent.describe", "version": "1", "doc": "describe {agentId} → Outcome<{identity, contract}> — the self-description slice: identity plus its resolved behavior contract." },
      { "kind": "engine", "id": "behavior.propose", "version": "1", "doc": "propose {id, version, preconditions, invariants, forbiddenActions, requiredCapabilities, recovery, provenance?} → Outcome<{rev, id, state}> — state forced staged, never live by construction." },
      { "kind": "engine", "id": "behavior.promote", "version": "1", "doc": "promote {contractId, evidence: VaultProvenanceRef[]} → Outcome<{rev}> — staged→active only with non-empty, resolving evidence (D-303 for behaviors: confidence never promotes alone)." },
      { "kind": "engine", "id": "behavior.rollback", "version": "1", "doc": "rollback {contractId} → Outcome<{quarantinedRev, reactivatedRev, rev}> — quarantines the active head, reactivates the newest staged|verified rev below it. Append-only: nothing deleted, both stay fetchable by rev." },
      { "kind": "engine", "id": "decision.record", "version": "1", "doc": "record {decisionId, subject, priorState, proposedState, actor, evidence?, parentDecisions} → Outcome<{rev, decisionId}> — genealogy as ordinary vault objects with refs; every parent must resolve (no dangling refs, G0 §4)." },
      { "kind": "engine", "id": "agent.exec", "version": "1", "doc": "exec {agentId, op, payload?, realizationRef?} → Outcome — B1a (D-327): admit once (contract active + version-pinned + call scope ⊆ capabilityToken + cited realization PROMOTED + law.check pre-check for the agent principal), settle once through the port, ledger once to ns agent id exec:<causationId> with admittedContractRev + quarantinedMidFlight (D-315 finish-then-halt). v0 verb scope is vault.* only." },
      { "kind": "engine", "id": "agent.snapshot", "version": "1", "doc": "snapshot {agentId} → Outcome<{agentId, identity, contract, contractRev, ledgerCursor, at}> — D-328a self-orientation: identity + resolved contract + this agent's exec-attempt cursor (bounded scan over ns agent exec: rows). READ-only; UNKNOWN for ghosts." },
      { "kind": "engine", "id": "agent.delegate", "version": "1", "doc": "delegate {parentAgentId, behaviorContractId, authority, task?, intent?, constraints?, deadline?, evidenceRequirements?, expectedOutputs?} → Outcome<{identity, scope, rev, delegationRev, childId}> — D-328b structured handoff: spawn under the parent's recorded authority (same subset math as spawn) + journal the envelope to vault ns control id delegation:<childId>. The child re-discovers its contract via describe/snapshot — envelope claims are hints, the vault is the authority." },
      { "kind": "engine", "id": "evolution.propose", "version": "1", "doc": "propose {id, version, preconditions, invariants, forbiddenActions, requiredCapabilities, recovery, actor, evidence?} → Outcome<{rev, evolutionRev, id, state}> — D-328b: identical staging gate as behavior.propose + mirror record to ns control id evolution:<id> (kind proposal). Thin alias, distinct name for genealogy; data only, never codegen." },
      { "kind": "engine", "id": "evolution.evaluate", "version": "1", "doc": "evaluate {contractId, evidence (non-empty), verdict: adopt|reject, note?, actor} → Outcome<{evolutionRev, verdict}> — D-328b: evidence-backed verdict to ns control. Informs promotion, never performs it. Unresolvable refs → EVALUATION_FAILED." },
      { "kind": "engine", "id": "evolution.promote", "version": "1", "doc": "promote {contractId, evidence (non-empty), actor} → Outcome<{rev, evolutionRev}> — D-328b: identical staged+resolving-evidence gates as behavior.promote + mirror to ns control (kind promotion). Confidence never promotes alone." },
      { "kind": "engine", "id": "evolution.rollback", "version": "1", "doc": "rollback {contractId, actor} → Outcome<{quarantinedRev, reactivatedRev, rev, evolutionRev}> — D-328b: identical rev-walk as behavior.rollback + mirror to ns control (kind rollback). Append-only, nothing deleted." },
      { "kind": "engine", "id": "delegate.chain", "version": "1", "doc": "chain {delegationId, carriedAuthority?, depthBudget?} → Outcome<{hops, depth, effectiveScope, authorityChain, expiresAt, text}> — READ (D-454, Ω-15): the vault-recomputed delegation fold over ns control chain rows — per-edge mechanical subset attenuation, clock-pinned expiry at every hop, subtree revocation discovery, the authorityChain digest citable by Ω-12 frames; a carried authority claim that disagrees with the fold refuses DELEGATE_ENVELOPE_UNVERIFIED (carried claims are routing hints, the vault rows are the truth)." },
      { "kind": "engine", "id": "delegate.revoke", "version": "1", "doc": "revoke {delegationId} → Outcome<{revokedAt, revokedThrough, cascade, liveDescendants}> — MUTATION (D-454, Ω-15): revoking a hop appends revoked rows for the whole descendant subtree in one append-only sweep (latest-wins per id, genealogy via refs); descendants discover at their next fold — gate-time pull, zero cached authority, zero live descendants by construction." },
      { "kind": "engine", "id": "adapt.propose", "version": "1", "doc": "propose {proposalId, target: {kind: behavior|realization|policy|lexicon, ref}, diffRef, rollbackPoint, census, censusBasis?, treatyChecks, window: {notBefore, notAfter}} → Outcome<{proposalId, status, censusDigest, rev}> — MUTATION (D-455, Ω-16): the ceremony's propose door — rollback point REQUIRED (ADAPT_ROLLBACK_POINT_MISSING), empty census only with its cited basis (ADAPT_CENSUS_UNCITED), self-governance proposals must count the ceremony among their dependents (ADAPT_SELF_GOVERNANCE); born proposed, never ratified by construction." },
      { "kind": "engine", "id": "adapt.ratify", "version": "1", "doc": "ratify {proposalId, census, treatyChecks?, ratifier} → Outcome<{proposalId, status, ratifier, rev}> — MUTATION (D-455, Ω-16): the signed flip — window-checked (ADAPT_WINDOW_CLOSED), census re-digested at the flip (ADAPT_CENSUS_UNCITED / ADAPT_BLAST_RADIUS_CHANGED), treaty-guarded (ADAPT_TREATY_UNCHECKED), signed by a human principal or a live auto-with-proof standing, both cited and resolved (ADAPT_SILENT_PROMOTION); the ratified row carries the chain of the yes." },
      { "kind": "engine", "id": "adapt.read", "version": "1", "doc": "read {proposalId?} → Outcome<{proposals, text} | {proposal, text}> — READ (D-455, Ω-16): the ceremony renders headless; rows before pixels." }
    ]
  },
  "dependencies": [],
  "capabilities": {
    "requested": ["port:vault.append@1", "port:vault.get@1", "port:vault.query@1", "port:invoke.check@1", "port:message.send@1", "port:law.forbidden.set@1", "port:law.check@1"],
    "justification": "Reads/writes agent, behavior, decision, and Phase-1 agency vault objects (append/get/query); agency.execute@1 invokes the D-452 live invocation gate and the fixed message.send@1 target; existing forbidden.set/law.check remain for the pre-existing agent.exec path. No host capabilities; no write path outside the vault. Granted per-composition by the user-signed Recipe, never by this manifest."
  },
  "runtime": { "tier": "worker-thread", "budget": { "cpuMs": 500, "memMB": 64 } },
  "contentHash": ""
}
