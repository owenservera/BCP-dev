# Vault Namespaces (A6)

One row per namespace: owner plugin, object shapes, writers, retention. New namespaces
get a row here *in the same commit that first writes them* — an undocumented namespace
is a namespace nobody can compact, query, or trust.

Conventions both writers and readers obey (enforced by `vivim.vault`, not by convention):
refs are `{ns, id, rev}` with integer rev ≥ 1 (`validate.ts` rejects anything else);
compaction never deletes a revision cited by a live object's refs; the changelog is
append-only forever while object revisions are latest-wins (hot) + cold fallback.

**The stronger compaction invariant (D-410, Core Phase item C — the F9
prerequisite, held explicitly as law, not prose):** compaction NEVER deletes a
revision, period — cited or not, hot or cold. Compaction moves revisions
hot→cold; it never removes them. Time travel (CIV-22–25, F9), the Exit
Manifest (CIV-30/31, F10), decision replay (CON-18), and scope-undo (CIV-24)
are folds over the full log; deleting any revision anywhere breaks projection
at arbitrary T and turns those features into data migrations. This is the
stronger form of the convention above, stated so a future compaction change
reads it before it ships — the falsifier stays §3.1's: if compaction ever
deletes (rather than moves) an uncited revision, the tactical map's biggest
row flips structural.

## Worked example: audit log vs current state (D-319)

Promotion of a candidate produces TWO writes with different retention needs:
- `discovery` ns, `promotion:<runId>` — the audit event (proof chain: probes, policy,
  gaps). Append-only forever; never updated, never compacted away while cited.
- `providers` ns, `realization:<archetype>:<provider>` — the current state (latest
  status). Latest-wins; each new verdict supersedes by new revision.
The realization cites the promotion event in its refs. Collapsing these into one
write would force a choice between auditability and freshness — hence two objects.

## The rows

| ns | Owner | Object ids | Writers | Retention |
|---|---|---|---|---|
| `email` | provider.email-file | message ids (`msg_*`), `capture:*` (spans) | provider (receive/send), director tick reads | Messages accumulate; `seen`/folder are data revisions, never deletes |
| `automation` | vivim.director | `rule:*`, `fired:*` | director (rules, tick ledger) | Ledger rows are authority (no-refire); kept while any rule lives |
| `nlcl` | vivim.director | `lexicon:*` | director.teach | Survives plugin swaps by design (D-218) |
| `discovery` | discovery-* engines | `candidates:*`, `mapping:*`, `promotion:*`, `graph:*`, `capture:*`, `trace:*`, `observation:*` | infer/map/verify/perceive/observe | Promotion events append-only (proof chain); graphs/captures cited by evidence refs (compaction honors) |
| `agent` | vivim.agent | `agent_*` identities, `exec:*` per-attempt ledger (`exec:<causationId>`) | agent.spawn (identities), agent.exec (ledger rows: admittedContractRev + quarantinedMidFlight + decision, settled and refused alike — D-327) | Append-only; lineage refs must resolve (dangling parents refused at write); exec rows kept while their agent lives |
| `behavior` | vivim.agent | contract ids (multi-rev: staged→active→quarantined) | propose/promote/rollback | All revs kept (rollback walks history; quarantine never deletes) |
| `decision` | vivim.agent | `dec-*` genealogy records | decision.record | Append-only; parent refs must resolve at write (G0 §4) |
| `providers` | vivim.providers (reads), discovery.verify/healing (writes) | `realization:<archetype>:<provider>` | verify (PROMOTED/REQUIRES_REDISCOVERY/TESTING); healing (DEGRADED/TESTING) | Latest-wins current state; cites `discovery` promotion events (see worked example) |
| `probe` | tests only | `p*` seeded proof records | test harnesses | Ephemeral; never read by product code |
| `law` | vivim.law | `forbidden:<principal>` (D-325: per-principal forbidden-action overlay records `{principal, ops, updatedAt}`) · `journal:<boot>-<seq>` (D-416/S3, the fold: law's narrative journal rows — one object per event, `{ts, source: vivim.law, op, principal, decision, reason, causationId, …}` including the D-411 intent citations `{intentRef, payloadHash}` and the D-412 principal events) | vivim.law (forbidden.set appends after the in-memory set — append failure aborts the set fail-closed; boot reloads once the vault is queryable; journal rows append wherever `port:vault.append@1` is granted — best-effort, a law decision is never blocked by a journal failure, the row is lost loudly) | Forbidden: latest-wins per principal; empty-`ops` tombstones preserve clears across restarts. Journal: append-only forever — one row per event, never revised, never compacted away (the narrative IS the evidence; the compaction law D-410-C protects it sight unseen) |
| `audit` | vivim.law (D-416/S3 — the audit-chain persistence point) | `audit-chain:<boot>-<seq>` (one row per drain: the FULL kernel audit-chain export `{verified, signerKeyId, publicKey, length, headHash, entries}` — each entry a signed ed25519 grant) | law.audit.drain@1 (requires `host.kernel.lens` + `port:vault.append@1`; the drain is read-only on the kernel, fail-closed on the vault append; the console's close sequence drains before host shutdown, best-effort) | Append-only forever — every drain is a full snapshot, rows never supersede; the kernel chain itself stays in-memory-and-authoritative, these rows are its persisted witnesses (drain cadence is a floor, not a ceiling) |
| `resolve` | vivim.director | `resolve:<decisionId>` (D-323: decision rev 1 `{decisionId, kind, capability, branch, reason, evidenceRefs, buildDecisionRef: "D-323"}` + outcome rev 2 `{status, execMs}`) | resolve.classify (rev 1) + resolve.report (rev 2); strategy.scorecard reads; vivim.chat writes chat-originated DETERMINISTIC verdicts (rev 1 only, D-359 — the ambiguous half's rows stay the director's) | Two revs of one object (verdict then outcome); ids without outcomes are decided-but-unreported |
| `control` | vivim.agent (writes), vivim.mind (reads) | `delegation:<childId>` (D-328b handoff envelopes), `evolution:<contractId>` (D-328b proposal→evaluation→promotion/rollback mirrors) | agent.delegate (delegations); evolution.propose/evaluate/promote/rollback (evolution log); control.describe counts `evolution:` rows | Latest-wins per id; behavior mechanics stay in ns `behavior` (control mirrors genealogy, never authority) |
| `intent` | vivim.intent | `intent:<hex>` (multi-rev: submitted→resolving→planned→awaiting_approval→executing→succeeded\|failed\|rejected\|cancelling\|cancelled; D-411: rev 1 carries the interpretation summary {text, canonical, reading, confidence, status} — the UNDERSTOOD artifact), `<hex>:res` (D-411: four-state resolution rows {kind: resolution, resolution: AMBIGUOUS\|REFUSED\|EXECUTED, intentRef, payloadHash?, text?, decision?, consentId?, outcome?} — AMBIGUOUS cites null intentRef by design), `amb:<hex>:res` (D-411: ambiguous attempts with no persisted artifact) | intent.submit (rev 1), intent.resolution (the `:res` family), intent.resolve/step.execute/cancel (later revs) | All revs kept — audit trail; cites resolve/law/plan refs; the law journal's D-411 rows cite back via {intentRef, payloadHash} | 
| `principal` | vivim.law (D-412, Core Phase S2 — the principal-identity seam) | `<principal-string>` (the id IS the principal; record `{principal, kind, registeredAt, state: active\|retired, retiredAt?, generation}`; **retention: forever — identity rows are permanent, the non-reuse substrate**) | law.principal.register (rev 1), law.principal.retire (the terminal append); law.consent.grant naming a principal ensures the row exists (vault caps required, fail-closed) | Retired is FOREVER: a retired id REFUSES re-registration (PRINCIPAL_REUSED) — the string can never become a different record; no crypto lives here yet (key-binding attaches to records later without re-typing keyed history) |
| `work` | vivim.run | `work_*` (durable Work records), `plan_*` (immutable per-Work plan snapshots), `attempt_*` (execution occurrences) | `vivim.run` durable Work ops (`work.create/get/list/transition/attempt.start/attempt.finish/reconcile/verify/cancel/recover`) through the existing vault writer | Latest Work/Plan/Attempt revisions are current state; every prior revision remains available under D-410; running Work is reconciled after process recovery rather than silently replayed |
| `intent-plan` | vivim.intent (writer: explicitly authorized plan-authoring path only — never a bare `intent.*` op) | `plan:<type>@<planVersion>` (immutable once promoted; versioned by promotion only) | Plan-authoring plugin/console only (narrower writer set than `intent`) | All revs kept forever — in-flight intents may cite specific plan revisions years later; templates never edited in place |
| `chat` | vivim.chat | `conv:*` (conversation rows, latest-wins), `msg:*` (messages, append-only ledger; D-358: `{id, conversationId, role, seq, content, providerId?, realizationRef?, streamRef?, createdAt}` — realizationRef = producing realization, streamRef = M1's chunk envelope when streamed), `idx:*` (D-378: per-conversation writer-maintained index rows `idx_<hex>`, latest-wins `{conversationId, count, entries:[{id, seq}]}`, bounded by CHAT_HISTORY_CAP, same-append-critical-section as the message), `refusal:*` (D-379: principal-refusal ledger rows `{conversationId, owner, caller, op, at}`) | chat.open (conversations), chat.append (messages + index rows), chat.history (reads + refusal ledger on fenced reads); chat.resolve writes ns `resolve`, never ns `chat` verdicts twice | Retention windows DECLARED per D-378: conversations hot 180d; conv superseded revs cold after 90d; refusal rows 400d; messages bound by CHAT_HISTORY_CAP=200/conversation (fail-closed refusal at cap on the indexed path — never silent truncation); mechanical enforcement of the windows is Wave3 with the full index build. Cross-principal reads refuse (REFUSED verdict) + ledger, never degrade (D-379) |
| `proposal` | forge.author (Wave 0, D-406; the future forge.emit.* emitters join as writers by amendment) | `file:<pluginId>/<path>` (one row per emitted file, latest-wins: pack.builder `proposal-artifact` shape `{schemaVersion, targetPath, artifactKind, contentHash, generatedBy, ledgerRef, authority, justification}` — `authority` is schema-pinned `"none"`, the row CANNOT carry authority) | forge.author.init@1 (append + read-back verify, one row per emitted file; the op refuses fail-closed if any row does not read back byte-identical) | Proposal ledger: append-only in effect (latest-wins per file id on re-emission of the same spec); rows cite `decision:D-406`; nothing in ns `proposal` is authority — emission confers none, promotion is a separate human decision (forge.tier's Wave 1+ scope); boot never reads this ns |
| `canvas` | **RESERVED — no writer yet** (explicit reservation per the D-332 discipline, like D-373's storage.kv and D-389's plan/saga exports): sole writer will be the canvas surface plugin when the canvas wave lands (CP-3, D-407 — ratified direction; the wave opens later) | *(none written yet — one design law holds at creation, not code: canvas row shapes carry the CRDT-fold semantics from the first row; sloppy first rows cannot be retro-CRDT'd)* | none — until the canvas surface plugin's first writer commit lands WITH this row's real shapes (an undocumented namespace is refused; a foreign writer to `ns canvas` is a named refusal) | To be declared with the first writer: versioned, restorable spatial snapshots — layout survives the app, the device, and the decade (CP-3) |
| `vault.recovery` | vivim.vault (D-432, Ω-0.5 — the crash-recovery ceremony's ledger) | `tear:<hash16>` (one row per quarantined torn tail: `{bootId, kind: torn-bytes\|uncommitted-intent\|committed-data-torn-commit, byteRange, quarantineHash, precedingCommit, refusal: VAULT_TORN_TAIL_QUARANTINED, sentence, badge: quarantined, ns?, id?, rev?, dataCommitted?, committedSeq?}`) | vault.recover@1 (the ceremony — also automatic on boot); vault.import@1 re-mints carried rows on fresh vaults | FOREVER — quarantined torn bytes are evidence; the sidecar-carrying artifacts under `<dataDir>/quarantine/` are their verbatim proof (auditRecovery checks both directions) |
| `vault.migrate` | vivim.vault (D-432 — the schema-migration ledger) | `plan:v<from>-v<to>` (migration plans `{kind: migration-plan, from, to, steps, census, censusDigest, rollbackPoint}`), `version:v<N>` (version rows `{version, kind: genesis-stamp\|version-row, rollbackPoint, planRef}` — v1's implicit genesis is stamped explicit at first migration) | vault.migrate.dryrun@1 (plans + genesis stamps), vault.migrate@1 (version rows) | FOREVER — rollback points cite real rows; history is the backup |
| `vault.compact` | vivim.vault (D-432 — the compaction ledger) | `plan:<digest16>` (census plans `{kind: compaction-plan, ns, keep, candidates, excluded, censusDigest, blobBytes}`), `receipt:<digest16>` (receipts `{kind: compaction-receipt, moved, kept, protected, censusDigest, reclaimedBytes}`) | vault.compact.dryrun@1 (plans), vault.compact@1 plan-driven path (receipts) | compact-shred-90d — rows forever (the tombstone zone), moved blobs shred at 90 days; the D-410-C never-delete invariant is enforced by the guard, not the retention |
| `law.conflict` | vivim.law (D-433, Ω-2.5 — the policy-lattice ledger) | `conflict-resolved:<a>--<b>:<hash16>` (`conflict.resolved@1` rows), `conflict-paradox:<a>--<b>:<hash16>` (`conflict.paradox@1` rows, `blockedUntil: amended`), `policy:<id>` (policy rows, append-only supersession history) | law.conflict.scan@1 (pair rows), policy.amend@1 (policy rows + re-scan) | FOREVER — paradoxes are blocked loudly until amended, never dropped; the amendment chain is append-only history |
| `compose.scan` | vivim.law (D-433 — the composition security-scan ledger) | `scan:<hash12>` (one row per scanned composition bytes: `{kind: compose.scan@1, compositionRef, manifestHash, findings, verdict}`) | compose.scan@1 (records; identical bytes replay the recorded row) | compose-scan-2y — activation requires a PASSING row matching the manifest hash; scan rows are the activation evidence |
| `intent.trace` | vivim.intent (D-434, Ω-2.5/Ω-2.6 — the derivation-trace store) | `utt:<hash16>` (one row per normalized utterance: the `intent.trace@1` shape `{utterance, steps, candidates, lexiconHits, disambiguations, resolvedIntent, resolverDigest, lexiconDigest, replayable}`) | intent.explain@1 (appends the trace; persistence best-effort — the trace is the answer) | intent-traces-1y — replay evidence; the diff op reads the stored row |
| `intent.lexicon` | vivim.intent (D-434 — the principal's words) | `lex:<hash16>` (entries `{entryId, principal, kind: alias\|pattern\|default\|disambiguation-rule, payload, scope, badge: GEN_PERSONAL, origin, createdAt, revokedAt?}` — revocation is the append-only tombstone) | intent.lexicon.add@1 (principal-authored), intent.lexicon.accept@1 (accepted proposals) — realizations NEVER write here (INTENT_LEXICON_AUTO_APPLY_REFUSED) | FOREVER — the principal's personal data by construction; the digest fold excludes revoked entries exactly |
| `intent.proposal` | vivim.intent (D-434 — the ML proposal waiting room) | `prop:<hash16>` (`{proposalId, realizationRef, proposedEntry, evidence, status: proposed\|accepted\|rejected\|expired, proposedAt, expiresAt}`) | intent.lexicon.propose@1 (any badged realization), intent.lexicon.accept@1 (status transitions) | lexicon-proposals-30d — an unreviewed suggestion is not a standing temptation; expiry is the retention law |
| `intent.audit` | vivim.intent (D-434 — the leakage-audit ledger) | `audit:<ts>` (audit runs `{ok, cases, findings}` — the INTENT_RESOLUTION_NONDETERMINISTIC tripwires and verdict drift) | intent.audit@1 | FOREVER — the audit trail of the ML-perceives-substrate-decides line |
| `exec.state` | vivim.run (D-435, Ω-3.5 — the plan-state ledger) | `stall:<planId>:<ts>` (EXEC_PLAN_STALLED events `{planId, stepId, blocker, stallDeadline, sentence}`) | exec.inspect@1 (the stall sweep), the stall detector | exec-state-90d — stall events age; the plan registry is the live truth |
| `exec.intervene` | vivim.run (D-435 — the intervention ledger) | `exec.intervene@1` rows (`{planId, verb: pause\|resume\|step\|cancel, by, at, risk}` — cancel is EXTERNAL_MUTATION), `exec.breakglass@1` rows (the OS-kill scars, EXEC_BREAKGLASS_EMPLOYED) | exec.intervene@1 (the gated verbs), breakGlass (the scar — loud, never silent) | FOREVER — every intervention is a row; the break-glass scar is the honest record of an ungated kill |
| `capability.lifecycle` | vivim.run (D-435 — the capability-deprecation ledger) | `<capability>` (lifecycle rows `{state: active\|deprecated\|sunset\|retired, successor, dependents[], censusDigest, migrationShim?, sunsetAt, decisionRef}` — census inputHash-pinned) | capability.deprecate@1 (the ceremony), useCapability (verdicts only) | FOREVER — no vanishing doors; the census digest pins the dependents the deprecation flagged |
| `surface.parity` | the parity sweep (D-436, Ω-4.5 — the sweep's ledger) | `sweep:<ts>` (parity reports `{pairs, driftCount, orphanCount, unreviewedNa}` — the live-probe fold's results) | surface.parity@1 (the sweep, probes injected) | FOREVER — the sweep history; drift events are evidence |
| `surface.na` | the N/A registry's vault mirror (D-436) | `na:<op>:<surface>` (reasoned exemption rows `{reason, author, at, revokedAt?}` — revocation returns the drift) | surface.na.declare@1 (one reasoned row per pair) | FOREVER — exemptions are reviewable history, never silently dropped |
| `surface.derivation` | the derivation registry's vault mirror (D-436) | `derived:<artifactPath>` (stamps `{surfaceRef, vocabularyDigest, derivationTool, derivedAt}`) | surface.derive@1 (the ONLY writer — the tamper gate's grant) | FOREVER — the stamp history; a foreign writer is tamper, named |
| `librarian.doc` | the librarian (D-437, Ω-5.5 — the governed-doc registry's vault mirror) | `doc:<docPath>` (manifest rows `{sources: [{path, sha256, role}], generator, era}`) | librarian.regenerate@1 (with the manifest update) | FOREVER — manifests are provenance history |
| `librarian.drift` | the librarian (D-437 — the drift ledger) | `drift:<ts>` (verify runs `{issues[], blastRadius-sorted}`) | librarian.verify@1, librarian.drift.read@1 | librarian-drift-2y — drift events age; the manifest pins do not |
| `perf.fixture` | the perf registry (D-438, Ω-6.5 — the fixture ledger) | `fixture:<fixtureId>` (fixture rows `{workload: {seed, opMix}, budget, variance, badge, deferredTo?}`) | perf.fixture.declare@1 | FOREVER — a fixture's budget is law from declaration |
| `perf.result` | the perf registry (D-438 — the result ledger) | `result:<fixtureId>:<ts>` (result rows `{metrics, environment, badge}` — GEN_ENVIRONMENT, never compared cross-machine as absolutes) | perf.run@1, perf.baseline.read@1 | perf-results-2y — results age; the budgets do not |
| `gov.validation` | the governance validator (D-439, Ω-7.5 — the validate ledger) | `validate:<ts>` (corpus verdicts `{issues[], checked}`) | gov.validate@1 (the six checks) | FOREVER — governance history is governance evidence |
| `gov.metrics` | the governance scorecard (D-439 — regenerable) | `metrics:<ts>` (scorecard snapshots — REGENERABLE, the fold is the truth) | gov.metrics@1 | gov-metrics-1y — a derived view, regenerable on demand |
| `watch` | the watch substrate (D-440, Ω-1) | `watch.registration@1` + `watch.observation@1` rows (registrations `{watchId, scope, subject, predicate, heartbeatMs?, budgetRef, action}`; observations `{at, observed, verdict, sentence?}`) | watch.register@1 (the door), watch.observe@1 (the evaluation record), the sweep | FOREVER — observations are evidence; the lease's WATCH_LEASE_EXPIRED rows ride the same ns |
| `badge` | the badge substrate (D-441, Ω-7) | `<badgeId>` rows (mint `{subjectRef, tier, assignedBy, generality, basis, era}`; promotions cite evidence; demotions scar forward) | badge.mint@1 (the kernel ceremony), badge.promote@1, badge.demote@1 | FOREVER — trust labels are history; nothing deletes, scars ride forward |
| `gov.budget` | the governor substrate (D-442, Ω-2) | `<budgetId>` rows (declarations `{principal, scope, limits, window}`; amendments cite declarationRef) | budget.declare@1 (sole writer) | FOREVER — budgets are law |
| `gov.burn` | the governor substrate (D-442) | `burn:<budgetId>:<tick>` (sampled consumption rows `{consumer, ops, cpuMs?, at}`) | the sampler (budget.consume@1's flush) | gov-burn-90d — burn samples age; the declarations do not |
| `ctx.window` | the context substrate (D-443, Ω-3) | `assembly:<digest>` (assembled-window rows `{sources[], rules, bounds, digest}` — the read-through cache) | the assembly fold (context.assemble@1) | ctx-windows-90d — windows age; the sources are the truth |
| `trust.mesh` | the trust substrate (D-444, Ω-4) | `key:<keyId>` (lineage rows `{principal, era: {notBefore, notAfter?}, fingerprint}`) | trust.bind@1 (sole writer), trust.revoke@1 (the terminal scar) | FOREVER — key lineage is identity history |
| `analytics` | the analytics substrate (D-445, Ω-5) | `obs:<ns>:<ts>` (observation rows `{eventNs, kind, principal?, payloadDigest}`) | analytics.record@1 (sole writer) | retention declared per event family — ANALYTICS_RETENTION_UNDECLARED refuses family-less rows |
| `layout` | the layout substrate (D-446, Ω-6) | `snapshot:<layoutId>:<n>` (placement snapshots `{tiles[], digest, parentSnapshot?}`; merge records name their parents) | layout.snapshot@1 (sole writer), the merge fold | FOREVER — spatial history; `canvas` stays reserved for the canvas-surface writer |
| `sim.scenario` | the simulation substrate (D-447, Ω-8) | `scenario:<scenarioId>` (pinned seeded scenarios `{seed, steps[], recordedFrom?}`) | sim.record@1 (sole writer) | FOREVER — scenarios are the rehearsal's law |
| `sim.run` | the simulation substrate (D-447) | `run:<scenarioId>:<ts>` (replay receipts `{steps' digests, verdict, advisoryForProduction: true}`) | sim.replay@1 (contained writes only) | sim-runs-2y — receipts age; the scenarios do not |
| `trust.keys` | the trust substrate (D-444, Ω-4) | `key:<keyId>` (key-lineage rows `{principal, era, fingerprint}` — tombstones included) | trust.bind@1, trust.revoke@1 | FOREVER — key lineage is identity history |
| `analytics.event` | the analytics substrate (D-445, Ω-5) | `obs:<ns>:<ts>` (observation rows `{eventNs, kind, principal?, payloadDigest}`) | analytics.record@1 | per class — default system-events-90d; ceremony/governor forever |
| `analytics.counter` | the analytics substrate (D-445) | `counter:<ns>:<kind>` (per-namespace counter folds — REGENERABLE) | the counter fold | analytics-counters-1y — regenerable caches |
| `aperture` | the aperture substrate (D-448, Ω-9) | `scope:<target>:<principal>` (scope rows `{width, setBy, at}`), `view:<hex>` (widening receipts) | aperture.scope.set@1 (sole scope writer), the projection fold | scopes FOREVER; receipts aperture-receipts-1y |
| `eval` | the partial-evaluation substrate (D-449, Ω-10) | `partial:<digest>` (partial results `{inputDigest, known, unknowns, determined, resultDigest}`), `residual:<digest>` (gated residual plans) | the partial fold, runResidual (gate-citation-only) | eval-partials-2y; residuals kept-while-cited |
| `liveness` | the liveness substrate (D-450, Ω-14) | `state:<tileId>` (state rows `{state, since, budgetRef?, wakeProof?}`), `proof:<tileId>:<ts>` (signed dormancy proofs) | liveness.set@1, liveness.wake@1 (the ceremonies) | states FOREVER; proofs liveness-proofs-30d |
| `privacy` | the aperture-privacy law (D-451, Ω-11) | `class:<ns>` (class rows `{level, setBy, at}`), `refusal:<ts>` (the universal fence's rows, the D-379 envelope generalized) | privacy.class.amend (the class writer), the fence | classes FOREVER; refusal rows privacy-refusals-400d |
| `invoke` | the invocation law (D-452, Ω-12) | `inv:<causationId>` (frame rows `{caller, behalf?, op, scope, authority, intentRef?}`) | the invocation ledger (invoke.check@1's record) | invoke-2y — frames age; the authority re-resolves live |
| `standing` | the standing law (D-453, Ω-13) | `standing:<grantId>` (radius rows `{principal, grantee, scope, context, expiresAt, evidence[], revokedAt?}`) | standing.grant@1, standing.revoke@1 | FOREVER — authority history is constitutional evidence |
| `adapt` | the adaptation-governance law (D-455, Ω-16) | `proposal:<proposalId>` (ceremony rows `{target, census[], treatyChecks[], status, window, ratifier, rollbackPoint}`) | adapt.propose@1, adapt.ratify@1 | FOREVER — self-change history is constitutional evidence |

## Not vault namespaces (common confusion)

- `law-journal.jsonl` — a FILE on disk via `host.journal.append`, not a vault ns.
  **D-416 (S3, the fold) — the transition artifact:** law's own narrative rows no
  longer land here wherever the fold is active (they ride vault ns `law`, id
  family `journal:` — see the ns row above); its remaining writer is the µhost's
  own transport gate rows (one allow/deny/require-consent row per risky routed op
  — the B5-frozen host write path: host LOC is flat at 1500/1500 with zero
  headroom, so the host's journal writes cannot move; the rows are
  near-duplicates of law's richer check rows, which now ride the vault). Readers:
  the law registry replays it at boot for legacy rows (compositions without the
  vault grant — spine, chat, the test rigs — still write ALL law rows here, the
  transition discipline); the web console reads the vault, not this file.
- `dev-vault/*` — local dev persistence for shipped compositions, not a namespace.

## Open questions

- Retention policy for `email` at scale (per-folder keep windows?) — undecided, no pressure yet.
- ~~`healing` writes to `providers` are specified (A4 remainder) but not yet implemented.~~ Closed by D-326: `discovery.heal@1` appends `realization:<archetype>:<provider>` with `DEGRADED` (drift, no admissible candidate) or `TESTING` (probation entry), superseding the prior rev and citing the drift observation; `none`/`promote` write nothing (verify owns promotion).
- Session bookkeeping (`session:<uuid>` in ns `providers`) is deferred with its consumer.
