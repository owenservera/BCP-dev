import{createHash,generateKeyPairSync,sign,verify,createPrivateKey,createPublicKey,randomBytes}from"node:crypto";import{readFile,writeFile,mkdir}from"node:fs/promises";import{join}from"node:path";import{canonicalize}from"./canonical-json.js";import type{AgentIdentity,CommonsEvent}from"./types.js";
export function uuidv7(){const b=randomBytes(16),m=BigInt(Date.now());b[0]=Number(m>>40n)&255;b[1]=Number(m>>32n)&255;b[2]=Number(m>>24n)&255;b[3]=Number(m>>16n)&255;b[4]=Number(m>>8n)&255;b[5]=Number(m)&255;b[6]=(b[6]&15)|112;b[8]=(b[8]&63)|128;const h=b.toString("hex");return`${h.slice(0,8)}-${h.slice(8,12)}-${h.slice(12,16)}-${h.slice(16,20)}-${h.slice(20)}`}
export const sha256=(v:string)=>createHash("sha256").update(v,"utf8").digest("hex");export const eventHash=(e:CommonsEvent)=>`sha256:${sha256(canonicalize(e))}`;function unsigned(e:CommonsEvent){const{signature:_s,...r}=e;return r}
export function signEvent<T>(e:CommonsEvent<T>,k:string):CommonsEvent<T>{return{...e,signature:sign(null,Buffer.from(canonicalize(unsigned(e))),createPrivateKey(k)).toString("base64url")}}export const verifyEvent=(e:CommonsEvent,k:string)=>verify(null,Buffer.from(canonicalize(unsigned(e))),createPublicKey(k),Buffer.from(e.signature,"base64url"));
export async function loadOrCreateIdentity(d:string,id:string){await mkdir(d,{recursive:true});const ip=join(d,"agent.json"),pp=join(d,"private-key.pem");try{return{identity:JSON.parse(await readFile(ip,"utf8")) as AgentIdentity,privateKeyPem:await readFile(pp,"utf8")}}catch{const{publicKey,privateKey}=generateKeyPairSync("ed25519"),publicKeyPem=publicKey.export({type:"spki",format:"pem"}).toString(),privateKeyPem=privateKey.export({type:"pkcs8",format:"pem"}).toString(),identity:AgentIdentity={agent_id:id,key_id:`${id}:ed25519:${uuidv7()}`,public_key_pem:publicKeyPem,algorithm:"ed25519",created_at:new Date().toISOString()};await writeFile(ip,JSON.stringify(identity,null,2)+"\n",{mode:0o600});await writeFile(pp,privateKeyPem,{mode:0o600});return{identity,privateKeyPem}}}
