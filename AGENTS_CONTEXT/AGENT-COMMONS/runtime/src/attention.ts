import type{AttentionPolicy,MessagePayload,MessageKind,AttentionLevel}from"./types.js";const L:Record<AttentionLevel,number>={AMBIENT:0,NORMAL:25,ATTENTION:60,URGENT:100};const K:Record<MessageKind,number>={OBSERVATION:0,QUESTION:8,HYPOTHESIS:3,PROPOSAL:8,REQUEST:20,OBJECTION:35,EVIDENCE_REFERENCE:10,STATUS:2,HANDOFF:45,DECISION_CANDIDATE:30,ANNOUNCEMENT:5};export const attentionScore=(m:MessagePayload,a:string)=>Math.min(100,L[m.attention.level]+K[m.kind]+(m.recipients.includes(a)?20:0)+(m.mentions.includes(a)?30:0)+(m.handoff_id?15:0));export function rankAttention(ms:readonly MessagePayload[],a:string,p:AttentionPolicy={}):MessagePayload[]{return[...ms].filter(m=>!p.minimum_level||L[m.attention.level]>=L[p.minimum_level]).sort((x,y)=>attentionScore(y,a)-attentionScore(x,a)||y.message_id.localeCompare(x.message_id))}