{
  "migration_id": "MIG-002-claude-send-message",
  "source_behavior": "VIVIM Claude send_message (manifest + old plugin class + unified interface: triple representation)",
  "required_invariants": ["I-1 ordering", "I-2 identity (message_start linkage)", "I-3 content byte-exact", "I-4 exactly-once intent", "I-5 redact-before-vault", "I-6 named failures", "I-7 typed-block provenance (NEW)"],
  "omega_target": {
    "contract_op": "message.send@1",
    "archetype": "message.send",
    "plugin": "provider-browser",
    "provider_class": "BROWSER_MEDIATED",
    "registry": "vivim-providers deriveRegistry (realization-driven, pure)"
  },
  "architectural_transformation": "VIVIM Claude endpoint/selector/SSE-parser/account knowledge -> Omega op-map refs + parser pins + session rows + realization record (same mechanism as MIG-001, different data); programs -> composition grants via existing ceremony; SSE typed blocks -> sequence-checked chunks with authoritative terminating result; reasoning/tool blocks -> chunk data kinds pending vocabulary (unresolved, NOT flattened silently)",
  "new_dependencies": ["parser pin covering session + manifest versions", "PROMOTED realization (verify-written)", "attached session row", "day-one fence registration", "block-kind vocabulary decision (new — defers to human/Ω design, not invented here)"],
  "removed_dependencies": ["VIVIM monolith import", "Prisma runtime dependency in Omega", "imperative selector code in core", "second orchestrator", "old-plugin vs unified-interface choice (retired, not ported)"],
  "preserved_semantics": ["send_message intent", "ordering/identity/content", "provider fallback + recovery intent INCLUDING the extra textarea-fallback edge", "SSE streaming with message_stop completion (stronger rule than #1)", "conversation persistence intent", "named-failure visibility", "typed content blocks as a requirement (I-7)"],
  "changed_semantics": ["execution passes 4 fail-closed bars (new refusals possible where legacy may have proceeded)", "evidence is vault-provenance shaped (new shape, same facts)", "streaming is sequence-law shaped (new enforcement, same order)", "seed-parser silent skip of malformed SSE lines becomes a logged parser event (defect corrected at the contract, not preserved)"],
  "unresolved_semantics": ["reasoning/tool-call block representation in Omega (chat.ts content is a flat string; streamRef counts but does not kind chunks)", "attachment handling (shared U-1)", "dedup/replay exactness (shared)", "textarea-fallback target existence", "auth recovery path", "triple-representation live authority (manifest vs old plugin vs unified interface)"]
}
