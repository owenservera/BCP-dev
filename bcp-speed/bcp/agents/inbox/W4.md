# INBOX W4 — directives for AGT-c1 (Lane C). Chester reads this every heartbeat.
# Owner/coordinator writes; agent obeys within EXP-2026-006 scope. Newest at bottom.

## 2026-09-22 — BOOTSTRAP ORDERS (fresh session starts here)
Your full orders are `agents/prompts/lane-c.md` — follow it verbatim from section 1 (bootstrap first, expect FAM-09.1 only, read DISC-001 before leasing). Gate verified OPEN. After bootstrap, subscribe to this inbox (read it every heartbeat; directives here override prior lane text within scope). No amendments currently — build per lane-c.md.

## 2026-09-22 — LOOP IS LAW (applies from bootstrap on)
LOOP PROTOCOL (lanes.md) binds you from your first release: after every release, do NOT stop and do NOT message the user — re-read inbox + lanes, run available, take the next leasable cap in EXP-2026-006 immediately (after 09.1 L1: adapter surface L1→L2, then import-path hardening). Report only in state. Stop only on: inbox STOP, zero leasable caps (log BLOCKED, 3 heartbeats, stand by), unresolvable REFUSED (flag require_human, keep heartbeat).
